fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pytest>=9.0.3,<10.0
httpx>=0.27,<1.0

# Milestone 3 (live web search). Installing this costs nothing; only calling
# the Tavily API with a real key does.
tavily-python>=0.5,<1.0

# Loads backend/.env for local development so API keys don't need to be
# exported by hand every session. .env itself is gitignored.
python-dotenv>=1.2.3,<2.0

# Milestone 4 (semantic retrieval). Local model, no API key — but a real,
# if one-time and cached, model download (~90MB) plus the torch dependency.
# Optional: the app runs fine without it (keyword-only retrieval); omit this
# line from your install if you want to skip the download entirely and set
# DISABLE_SEMANTIC_RETRIEVAL=1.
sentence-transformers>=5.0,<6.0
# A security floor, not a feature: transformers 4.x has known advisories fixed only in 5.x, and
# sentence-transformers 3.x/4.x would pin it to 4.x. Verified with this project's semantic retriever.
transformers>=5.10,<6.0

# Translation of foreign-language evidence into English (a place in Japan or
# Germany is mostly written about in that language). Free and local — no API
# key, nothing leaves the machine — but a heavy install, and each language's
# pack (~100MB) downloads on first use. Optional like sentence-transformers:
# omit these two lines, or set DISABLE_TRANSLATION=1, to skip it entirely.
argostranslate>=1.9,<2.0
langdetect>=1.0.9,<2.0
